version: "3.8"

services:
<<<<<<< HEAD
  spring-app:
    build: .
    container_name: spring_container_2
    restart: always
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://host.docker.internal:3306/campuslearning
      SPRING_DATASOURCE_USERNAME: root       # đổi nếu không dùng root
      SPRING_DATASOURCE_PASSWORD: dungcon2005     # password MySQL hiện có
    networks:
      - app-net
    volumes:
      - C:\Esclipe_Web\campuslearning\src:/app/src
      - C:\Esclipe_Web\campuslearning\pom.xml:/app/pom.xml
      - ~/.m2:/root/.m2
      - ./uploads:/app/uploads

networks:
  app-net:
=======
  # 🔹 MySQL
  mysql:
    image: mysql:8.0
    container_name: mysql_container
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: dungcon2005
      MYSQL_DATABASE: campuslearning
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - app-net
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  # 🔹 RabbitMQ
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq_container
    restart: always
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    networks:
      - app-net
    healthcheck:
      test: ["CMD", "rabbitmqctl", "status"]
      interval: 10s
      timeout: 5s
      retries: 5

  # 🔹 Redis
  redis:
    image: redis:7-alpine
    container_name: redis_container
    restart: always
    ports:
      - "6379:6379"
    networks:
      - app-net

  redis-comment:
    image: redis:7-alpine
    container_name: redis_comment_container
    restart: always
    ports:
      - "6380:6379"
    networks:
      - app-net

  redis-conversation:
    image: redis:7-alpine
    container_name: redis_conversation_container
    restart: always
    ports:
      - "6381:6379"
    networks:
      - app-net

  redis-message:
    image: redis:7-alpine
    container_name: redis_message_container
    restart: always
    ports:
      - "6382:6379"
    networks:
      - app-net

  redis-story:
    image: redis:7-alpine
    container_name: redis_story_container
    restart: always
    ports:
      - "6383:6379"
    networks:
      - app-net

  # 🔹 Spring Boot (build từ source code clone về)
  spring-app:
    build:
      context: ./BackEnd-CampusLearningUser
      dockerfile: Dockerfile
    container_name: spring_container_2
    restart: always
    depends_on:
      mysql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_started
      redis-comment:
        condition: service_started
      redis-conversation:
        condition: service_started
      redis-message:
        condition: service_started
      redis-story:
        condition: service_started
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/campuslearning
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: dungcon2005
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SPRING_JPA_SHOW_SQL: "false"
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_RABBITMQ_USERNAME: guest
      SPRING_RABBITMQ_PASSWORD: guest
      SPRING_REDIS_HOST: redis
      SPRING_REDIS_PORT: 6379
      SPRING_REDIS_COMMENT_HOST: redis-comment
      SPRING_REDIS_COMMENT_PORT: 6380
      SPRING_REDIS_CONVERSATION_HOST: redis-conversation
      SPRING_REDIS_CONVERSATION_PORT: 6381
      SPRING_REDIS_MESSAGE_HOST: redis-message
      SPRING_REDIS_MESSAGE_PORT: 6382
      SPRING_REDIS_STORY_HOST: redis-story
      SPRING_REDIS_STORY_PORT: 6383
    networks:
      - app-net
    volumes:
      - ./uploads:/app/uploads

  # 🔹 Prometheus
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    volumes:
      - ./k8s:/etc/prometheus
    ports:
      - "9090:9090"
    networks:
      - app-net
    restart: always
  
  # 🔹 Grafana
  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
    ports:
      - "3000:3000"
    depends_on:
      - prometheus
    networks:
      - app-net
    restart: always

# 🔹 Networks
networks:
  app-net:

# 🔹 Volumes
volumes:
  mysql_data:
>>>>>>> 923e3092c89befcef8151ac54e3c33b5f467d36c
